prompt: "Replace the fixed-amount Razorpay Subscription autopay with a UPI Autopay mandate plus a daily escalating recurring debit: the amount steps up by the user's chosen daily_plan each day and resets every 30 days."
repo: "."
tasks_file: "./sandbox/tasks.json"
test_command: "source scripts/env.sh && python -m pytest api/tests -q"
sandbox_root: "./sandbox"
iter_strategy: "branches"
pr_mode: "none"
base_branch: "swe/20260814"
task_budget: 6
patience: 2
quality_thresholds: "8 / 100 / 60 / 4 / 400"
